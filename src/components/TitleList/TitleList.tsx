import { TitleListProps } from "./titleList.type";

function TitleList(props: TitleListProps) {
  const { title, list } = props;

  return <div className="w-64 h-96 border-4"></div>;
}

export default TitleList;
